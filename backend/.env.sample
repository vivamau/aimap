PORT=3000
NODE_ENV=development
# Where the public-facing GeoJSON is written. Path is relative to the repo root.
PUBLIC_GEOJSON_PATH=data/ai-models.geojson
# Allowed CORS origins, comma-separated. Leave empty for same-origin only.
CORS_ORIGINS=
